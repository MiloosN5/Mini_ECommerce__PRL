import Page from "../components/Page";
import HeroSection from "../sections/HeroSection";

const HomePage = () => {
  return (
    <Page blockPrefix="hero">
      <HeroSection />
    </Page>
  );
};

export default HomePage;
