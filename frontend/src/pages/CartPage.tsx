import Page from "../components/Page";
import CartSection from "../sections/CartSection";

const CartPage = () => {
  return (
    <Page blockPrefix="cart">
      <CartSection />
    </Page>
  );
};

export default CartPage;
